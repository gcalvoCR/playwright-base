import { Locator } from '../types';

/// -------------------- Logo --------------------------- ///
export interface LogoComponent {
    component: Locator;
}

const Logo: LogoComponent = {
    component: '#shc-logo'
};

export default Logo;
